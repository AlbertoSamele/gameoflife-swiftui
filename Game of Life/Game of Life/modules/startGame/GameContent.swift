import Foundation

struct GameContent {
  let randomPopulatePrompt = "Random"
  let playPrompt = "Play"
  let resetPrompt = "Reset"
  let clearPrompt = "Clear"
  let setupWarning = "Populating universe. Hold tight."
  let teardownWarning = "Killing off cells. Brace for impact."
}
